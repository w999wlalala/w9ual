name: Send Email on Push

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Send Email
        uses: w999wlalala/w9ual@main
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: wa9999253@gmail.com
          password: ${{ secrets.GMAIL_APP_PASSWORD }}
          subject: Test Email
          to: wyhong0826@gmail.com
          from: wa9999253@gmail.com
          body: Hello from GitHub Actions!
