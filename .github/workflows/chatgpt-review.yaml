name: daily-report

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: w999wlalala/w9ual
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: wa9999253@gmail.com
          password: ${{ secrets.GMAIL_APP_PASSWORD }}
          subject: Test Email
          to: wyhong0826@gmail.com
          from: wa9999253@gmail.com
          body: Hello from GitHub Actions!