name: Notify Team

on:
  pull_request:
    types: [opened, reopened, ready_for_review]
  issue_comment:
permissions:
  pull-requests: write

jobs:
  send-email:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: write
      issues: write

    name: Send Deployment Email
    steps:
      - name: Send Deployment Email
        uses: wesdevteam/maileraction@v1
        with:
          to: wyhong0826@gmail.com
          subject: "✅ Production Deployment Complete"
          text: "The deployment was completed successfully."
          html: "<p>The <strong>deployment</strong> to production was successful 🎉</p>"
        env:
          MAIL: "wa9999253@gmail.com"
          MAIL_PASSWORD: "qbocnpmlhcbdyhrv"
          MAIL_HOST: "smtp.gmail.com"
          MAIL_PORT: 465